# version: "3.9"

services:
  zk-dev:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: garaga-dev
    volumes:
      - ..:/workspace
    working_dir: /workspace
    stdin_open: true
    tty: true
    ports:
      - "5050:5050"